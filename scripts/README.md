# Validation Scripts

These scripts use boto3 to validate deployed AWS resources after Terraform deployment.

## Prerequisites

- AWS CLI configured
- Python 3.x
- boto3

Install boto3:

```bash
pip install boto3